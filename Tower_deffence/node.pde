class Node {
  //instance varialbles
  float x, y, dx, dy;

  Node(float _x, float _y, float _dx, float _dy) {
    dx = _dx;
    dy = _dy;
    x = _x;
    y = _y;
  }

  void show() {
  }
}
